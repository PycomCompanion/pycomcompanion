<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MaterialDrawerTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/md_grey_800</item>
        <item name="colorPrimaryDark">@color/md_grey_850</item>
        <item name="colorAccent">@color/md_blue_800</item>
        <item name="android:colorBackground">@color/md_grey_850</item>
        <item name="material_drawer_selected_text">@color/md_blue_800</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="TextAppearance.App.TextInputLayout" parent="@android:style/TextAppearance">
        <!-- Make more dynamic -->
        <item name="android:textColor">@color/md_blue_800</item>
    </style>

</resources>
